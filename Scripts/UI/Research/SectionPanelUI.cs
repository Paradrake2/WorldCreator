using UnityEngine;

public class SectionPanelUI : MonoBehaviour
{
    [SerializeField] private GameObject creationSectionButtonPrefab;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
